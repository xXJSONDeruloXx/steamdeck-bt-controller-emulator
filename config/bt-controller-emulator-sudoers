# Allow bt-controller-emulator to run btmgmt commands through secure wrapper script
# This wrapper validates inputs and restricts operations to only what's needed
%bluetooth ALL=(root) NOPASSWD: INSTALL_DIR_PLACEHOLDER/scripts/bt-controller-emulator-btmgmt.sh *
